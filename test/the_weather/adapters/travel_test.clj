(ns the-weather.adapters.travel-test
  (:require [the-weather.adapters.travel :as adapters.travel]
            [clojure.test :refer :all]
            [schema.test :as s]))

(s/deftest tabular->maps-test

           )

